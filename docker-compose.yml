services:
  postgres:
    image: postgres:16-alpine
    container_name: ozon-erp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ozon_erp
      POSTGRES_PASSWORD: ozon_erp_password
      POSTGRES_DB: ozon_erp
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: ozon-erp-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
